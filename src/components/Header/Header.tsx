import React from 'react';
import style from './Header.module.scss';

const Header: React.FC = () => {
  return <header className={style.header}>Исторические даты</header>;
};

export default Header;
